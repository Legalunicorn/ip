package bingus.command;

/**
 * Identifies the supported command categories used by the application and UI.
 */
public enum CommandType {
    ADD,
    DELETE,
    EXIT,
    FIND,
    INVALID,
    LIST,
    MARK,
    NONE,
    UPDATE
}
